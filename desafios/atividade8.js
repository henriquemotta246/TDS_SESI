// crie uma função que receba dois parametros : o valor em reais e a taxa de cambio no dia 

function conversor(valor, taxa){
    console.log(`${valor}` * `${taxa}`);
}
let valor = 10;
let taxa = 5.50;
conversor(valor, taxa);
